name: Export Xray Config

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run config generator
        run: python generate_config.py --export-only --output config.json

      - name: Upload config.json as artifact
        uses: actions/upload-artifact@v4
        with:
          name: xray-config
          path: config.json
